import asyncio

id = 0

class P:
	def __init__(self, id_t, ):
		self.id_t = id_t
		self.k = 2

	async def cs(self):
		self.id = 0
		await self.A()

	async def wait(self):
		if self.id == 0:
			await self.req()
		if self.x >  and id == pid:
			await self.cs()

	async def req(self):
		if self.x = k:
			await self.wait()

	async def A(self):
		if self.id == 0:
			await self.req()

loop = asyncio.get_event_loop()
loop.run_until_complete()
